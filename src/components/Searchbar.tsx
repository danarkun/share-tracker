import React, { useState } from 'react'

export const Searchbar = () => {
    const [text, setText] = useState('');

    // const onSubmit = () => {

    // }

    return (
        <div>

        </div>
    )
}
